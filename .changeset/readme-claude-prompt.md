---
"@drmahdikazempour/agent-stack": patch
---

v0.3.1 — docs: add a copy-paste Claude Code prompt at the top of the README that runs the full flow (init --all → audit + doctor → measure/token tracking → commit + push to main). Republished so the npm package page shows it.
