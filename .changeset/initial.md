---
"agent-stack": minor
---

Initial v0.1.0: one-shot `init` (detect → install → generate → wire → activate → baseline), `code`/`review`/`multimodal`/`spec` profiles, hook merger as sole settings.json writer, Claude Code + Cursor generation, and the `audit`/`optimize`/`doctor`/`measure`/`profile`/`graph`/`handoff`/`sync`/`uninstall` maintenance commands.
